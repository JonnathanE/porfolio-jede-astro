---
interface Props {
  class?: string;
  id?: string;
}
const { class: className, id } = Astro.props;
---

<section id={id} class={`w-full px-[12%] py-10 scroll-mt-20 ${className}`}>
  <slot />
</section>
